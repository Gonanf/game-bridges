[project]
name = "chess-hs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Chaos", email = "gabysolotorevsky100@gmail.com" }]
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "python-chess>=1.999",
    "websockets>=12.0",
]

# [project.scripts]
# chess-hs = "chess-hs:main"
#
# [build-system]
# requires = ["uv_build>=0.12.7,<0.13.0"]
# build-backend = "uv_build"
